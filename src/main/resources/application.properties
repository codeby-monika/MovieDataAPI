# Application name
spring.application.name=moviedataapi

# Server port (Render will override this automatically)
server.port=8080

# Logging (optional but helpful)
logging.level.org.springframework.web=INFO
